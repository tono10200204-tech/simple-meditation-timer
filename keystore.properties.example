# Copy this to keystore.properties (gitignored) and fill in the real values.
# storeFile may start with ~ for a path under your home directory, or be a
# path relative to the repository root (this file's own location).
storeFile=~/simple-meditation-timer-release.jks
storePassword=
keyAlias=smt
keyPassword=
