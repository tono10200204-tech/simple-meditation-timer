package com.shl.meditation.ui.theme

import androidx.compose.ui.graphics.Color

// Paper and ink. There is deliberately no accent colour anywhere in the app —
// selection and emphasis are carried by weight and opacity instead, which also
// keeps the UI readable without relying on colour.

val Paper = Color(0xFFFAFAF7)
val PaperRaised = Color(0xFFF2F1EC)
val PaperInk = Color(0xFF1A1A18)
val PaperInkMuted = Color(0xFF8C8C85)
val PaperLine = Color(0xFFCFCEC4)

val Night = Color(0xFF0D0D0C)
val NightRaised = Color(0xFF171716)
val NightInk = Color(0xFFEDECE7)
val NightInkMuted = Color(0xFF6B6B65)
val NightLine = Color(0xFF37362F)
